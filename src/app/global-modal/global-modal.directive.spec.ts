import {GlobalModalDirective} from './global-modal.directive';

describe('GlobalModalDirective', () => {
  it('should create an instance', () => {
    const directive = new GlobalModalDirective();
    expect(directive).toBeTruthy();
  });
});
